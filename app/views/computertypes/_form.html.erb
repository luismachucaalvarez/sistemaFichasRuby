<%= form_with(model: computertype, local: true) do |form| %>
  <% if computertype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(computertype.errors.count, "error") %> prohibited this computertype from being saved:</h2>

      <ul>
      <% computertype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
